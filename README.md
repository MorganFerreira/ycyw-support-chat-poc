# YcywSupportChatPoc
Cette application est un POC pour Your Car Your Way dont le but est de démontrer la faisabilité de la fonctionnalité de chat en ligne entre le service client et un client.

## Pré-requis
This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 19.1.7.

## Development server

To start a local development server, run:

```bash
ng serve
```

Once the server is running, open your browser and navigate to `http://localhost:4200/`. The application will automatically reload whenever you modify any of the source files.

## Building

To build the project run:

```bash
ng build
```

This will compile your project and store the build artifacts in the `dist/` directory. By default, the production build optimizes your application for performance and speed.


## Licence

Développé dans le cadre du projet 13 d'OpenClassrooms: Définissez une solution fonctionnelle et concevez l’architecture d’une application
